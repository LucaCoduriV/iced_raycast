[package]
name = "gif_plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin_api = { path = "../../plugin_api" }
abi_stable = "0.11"
ureq = { version = "2", features = ["json"] }
serde_json = "1"
